import FormLoginAsisten from "../../components/form/FormLoginAsisten"

export default function LoginAsisten (){
    return (
        <>
        <FormLoginAsisten label1={'nama'} label2={'password'} gambar={'logo-asisten.svg'}/>
        </>
    )
}